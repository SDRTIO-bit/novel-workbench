# NARRATIVE_ROUTE_CONVERGENCE_V1 results

For every blind ID in `blind_review_queue.json`, record the external detector's three character ratios and orange spans.

Acceptance gates per route:
- Median human rate >=60% on 6 scenes
- >=2/3 scenes better than baseline
- >=2 of 3 replicas >=60%
